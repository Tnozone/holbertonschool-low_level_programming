#include"main.h"
/**
 * _islower - Entry point
 * description - prints minutes of a day
 * Return: Always 0 (Success)
 */

void jack_bauer(void)
{
int x;
int h = 24;
int m = 60;

for (h in x)
{
for (m in h)
{
_putchar(h, ':', m);
}
}
}
