#include"main.h"
/**
 * _islower - Entry point
 * description - print alphabet
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
char c;

_putchar(islower(c));
if (true)
{
return (1);
}
if (false)
{
return (0);
}
}
