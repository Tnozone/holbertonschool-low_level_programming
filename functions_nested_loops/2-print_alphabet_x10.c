#include<stdio.h>
/**
 * main - Entry point
 * description - print alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
char alph;
int i = 0;
for (alph = 'a'; alph <= 'z'; alph++)
{
while (1 < 10)
{
_puchar(alph);
i++;
}
}
Return (0);
}
