#include<main.h>
/**
 * main - Entry point
 * description - print alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
char alph;
for (alph = 'a'; alph <= 'z'; alph++)
{
_putchar(alph);
}
_putchar('\n');
Return (0);
}
