#include"main.h"
/**
 * _puts_recursion - Entry point
 * description - compares two strings
 * @s: string
 * Return: Sucess
 */

void _puts_recursion(char *s)
{
_putchar(*c); 
}

int main(void)
{
_puts_recursion(*s);
}
