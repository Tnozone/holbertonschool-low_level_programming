#include"main.h"
/**
 * *string_toupper - Entry point
 * description - prints a string
 * @s: pointer to string
 * Return: string
 */

char *string_toupper(char *s)
{
int i = 0;

while (s[i] != '\0')
{
if (*s = a-z)
{
s = s - 32);
}
i++;
}

return (s);
}
