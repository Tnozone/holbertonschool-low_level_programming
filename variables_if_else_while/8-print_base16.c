#include<stdio.h>
/**
 * main - Entry point
 * description - prints alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	int dig = 0x;

	for (dig = 0; dig <= 'f'; dig++)
	putchar(%x dig + '0');
	putchar('\n');
	return (0);
}
