#include<stdio.h>
/**
 * main - Entry point
 * description - prints alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	int dig = 0x;

	putchar(%x dig + '0', digit);
	putchar('\n');
	return (0);
}
