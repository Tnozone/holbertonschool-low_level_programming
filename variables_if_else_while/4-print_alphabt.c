#include<stdio.h>
/**
 * main - Entry point
 * description - prints alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
char al;

for (al = 'a'; al <= 'z'; al++)
{
if (al == 'd')
{
continue;
}
if (al == 'd')
{
continue;
}
putchar(al);
}
putchar('\n');
return (0);
